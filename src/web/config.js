/**
 * ELEN — Global configuration
 *
 * Set ELEN_DEBUG to false to hide all UI chrome (sidebar, action bar,
 * status bar, version tag, splash) except the battery indicator.
 * The gallery close button is also hidden when false.
 */
window.ELEN_DEBUG = false; // Set to true to show debug UI chrome and gallery close button
